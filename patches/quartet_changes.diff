diff --git a/kernels/CMakeLists.txt b/kernels/CMakeLists.txt
index 2685369..62dc415 100644
--- a/kernels/CMakeLists.txt
+++ b/kernels/CMakeLists.txt
@@ -3,7 +3,7 @@ project(EDEN CUDA CXX)
 
 include(FetchContent)
 set(CMAKE_CUDA_STANDARD 20)
-set(CMAKE_CUDA_ARCHITECTURES "120a")
+set(CMAKE_CUDA_ARCHITECTURES "100a")
 
 include_directories(csrc)
 
diff --git a/kernels/csrc/binding.cpp b/kernels/csrc/binding.cpp
index 89fb62e..bb9f24e 100644
--- a/kernels/csrc/binding.cpp
+++ b/kernels/csrc/binding.cpp
@@ -28,17 +28,17 @@ using CudaArray = nb::ndarray<Args..., nb::c_contig, nb::device::cuda>;
 
 void group_transform_128_eden(__nv_fp4x2_storage_t* y, __nv_fp8_e4m3* scales_fp8, float* global_scale_ptr,
                               nv_bfloat16* scratch_scales, unsigned* max_scale, const nv_bfloat16* h, const nv_bfloat16* x,
-                              long seed, float fp4_max, float fp8_max, int M, int N, bool transpose);
+                              long seed, float fp4_max, float fp8_max, int M, int N, bool transpose, cudaStream_t stream);
 extern void group_transform_128(nv_bfloat16* y, const nv_bfloat16* trans, const nv_bfloat16* x, int M, int N, bool transpose);
 
-void eden_fp4(__nv_fp4x4_e2m1* y_ptr, __nv_fp8_e4m3* scale_ptr, const nv_bfloat16* x_ptr, const float* amax_ptr, float scale_override, long seed, long nelem);
-void rtn_fp4(__nv_fp4x4_e2m1* y_ptr, __nv_fp8_e4m3* scale_ptr, float* global_scale_ptr, const nv_bfloat16* x_ptr, const float* amax_ptr, float scale_override, long nelem);
-void four_six_fp4(__nv_fp4x4_e2m1* y_ptr, __nv_fp8_e4m3* scale_ptr, float* global_scale_ptr, const nv_bfloat16* x_ptr, const float* amax_ptr, float scale_override, long nelem);
+void eden_fp4(__nv_fp4x4_e2m1* y_ptr, __nv_fp8_e4m3* scale_ptr, const nv_bfloat16* x_ptr, const float* amax_ptr, float scale_override, long seed, long nelem, cudaStream_t stream);
+void rtn_fp4(__nv_fp4x4_e2m1* y_ptr, __nv_fp8_e4m3* scale_ptr, float* global_scale_ptr, const nv_bfloat16* x_ptr, const float* amax_ptr, float scale_override, long nelem, cudaStream_t stream);
+void four_six_fp4(__nv_fp4x4_e2m1* y_ptr, __nv_fp8_e4m3* scale_ptr, float* global_scale_ptr, const nv_bfloat16* x_ptr, const float* amax_ptr, float scale_override, long nelem, cudaStream_t stream);
 void dequant_tp_had_quant(
     __nv_fp4x2_storage_t* y, __nv_fp8_e4m3* scales_fp8, float* global_scale_ptr,
     nv_bfloat16* scratch_scales, unsigned* max_scale, const nv_bfloat16* h,
     const __nv_fp4x2_storage_t* x, const __nv_fp8_e4m3* x_scales, const float* x_global_scale,
-    long seed, float fp4_max, float fp8_max, int M, int N);
+    long seed, float fp4_max, float fp8_max, int M, int N, cudaStream_t stream);
 
 void group_transform_128_binding(
     const CudaArray<nb::shape<-1, -1>>& y,
@@ -70,6 +70,8 @@ void group_transform_128_binding(
         x.shape(0), x.shape(1), transpose);
 }
 
+// ... (imports)
+
 void group_transform_128_eden_binding(
     const CudaArray<unsigned char>& y,
     const CudaArray<>& group_scales,
@@ -79,8 +81,9 @@ void group_transform_128_eden_binding(
     const CudaArray<nb::ro, nb::shape<128, 128>>& h,
     const CudaArray<nb::ro>& x,
     const nb::ndarray<long, nb::c_contig, nb::device::cpu, nb::shape<>>& seed,
-    float fp4_max, float fp8_max, bool transpose)
+    float fp4_max, float fp8_max, bool transpose, uintptr_t stream_ptr)
 {
+    cudaStream_t stream = reinterpret_cast<cudaStream_t>(stream_ptr);
     nb::dlpack::dtype bf16_dt{static_cast<std::uint8_t>(nb::dlpack::dtype_code::Bfloat), 16, 1};
 
     CHECK_EQ(x.ndim(), 2ul);
@@ -113,7 +116,7 @@ void group_transform_128_eden_binding(
         reinterpret_cast<const nv_bfloat16*>(h.data()),
         reinterpret_cast<const nv_bfloat16*>(x.data()),
         *seed.data(),  fp4_max, fp8_max,
-        x.shape(0), x.shape(1), transpose);
+        x.shape(0), x.shape(1), transpose, stream);
 }
 
 void eden_fp4_binding(
@@ -122,9 +125,11 @@ void eden_fp4_binding(
     const CudaArray<nb::ro, nb::shape<-1, 16>>& inp,
     const CudaArray<float, nb::ro, nb::shape<1>>& amax_ptr,
     float scale_override,
-    long seed
+    long seed,
+    uintptr_t stream_ptr
     )
 {
+    cudaStream_t stream = reinterpret_cast<cudaStream_t>(stream_ptr);
     nb::dlpack::dtype bf16_dt{static_cast<std::uint8_t>(nb::dlpack::dtype_code::Bfloat), 16, 1};
 
     CHECK_EQ(out.shape(0), inp.shape(0));
@@ -135,7 +140,7 @@ void eden_fp4_binding(
         reinterpret_cast<__nv_fp8_e4m3*>(scales.data()),
         reinterpret_cast<const nv_bfloat16*>(inp.data()),
         amax_ptr.data(),
-        scale_override, seed, inp.size());
+        scale_override, seed, inp.size(), stream);
 }
 
 void four_six_fp4_binding(
@@ -144,9 +149,11 @@ void four_six_fp4_binding(
     const CudaArray<float, nanobind::shape<>>& global_scale,
     const CudaArray<nb::ro>& inp,
     const CudaArray<float, nb::ro>& amax_ptr,
-    float scale_override
+    float scale_override,
+    uintptr_t stream_ptr
     )
 {
+    cudaStream_t stream = reinterpret_cast<cudaStream_t>(stream_ptr);
     nb::dlpack::dtype bf16_dt{static_cast<std::uint8_t>(nb::dlpack::dtype_code::Bfloat), 16, 1};
 
     CHECK_EQ(out.shape(0), inp.shape(0));
@@ -159,7 +166,7 @@ void four_six_fp4_binding(
         global_scale.data(),
         reinterpret_cast<const nv_bfloat16*>(inp.data()),
         amax_ptr.data(),
-        scale_override, inp.size());
+        scale_override, inp.size(), stream);
 }
 
 void rtn_fp4_binding(
@@ -168,9 +175,11 @@ void rtn_fp4_binding(
     const CudaArray<float, nanobind::shape<>>& global_scale,
     const CudaArray<nb::ro>& inp,
     const CudaArray<float, nb::ro>& amax_ptr,
-    float scale_override
+    float scale_override,
+    uintptr_t stream_ptr
     )
 {
+    cudaStream_t stream = reinterpret_cast<cudaStream_t>(stream_ptr);
     nb::dlpack::dtype bf16_dt{static_cast<std::uint8_t>(nb::dlpack::dtype_code::Bfloat), 16, 1};
 
     CHECK_EQ(out.shape(0), inp.shape(0));
@@ -183,7 +192,7 @@ void rtn_fp4_binding(
         global_scale.data(),
         reinterpret_cast<const nv_bfloat16*>(inp.data()),
         amax_ptr.data(),
-        scale_override, inp.size());
+        scale_override, inp.size(), stream);
 }
 
 
@@ -198,8 +207,9 @@ void dequant_tp_had_quant_binding(
     const CudaArray<unsigned char, nb::ro>& x_micro_scales,
     const CudaArray<float, nb::ro, nb::shape<>>& x_global_scale,
     const nb::ndarray<long, nb::c_contig, nb::device::cpu, nb::shape<>>& seed,
-    float fp4_max, float fp8_max)
+    float fp4_max, float fp8_max, uintptr_t stream_ptr)
 {
+    cudaStream_t stream = reinterpret_cast<cudaStream_t>(stream_ptr);
     nb::dlpack::dtype bf16_dt{static_cast<std::uint8_t>(nb::dlpack::dtype_code::Bfloat), 16, 1};
 
     CHECK_EQ(x.ndim(), 2ul);
@@ -229,7 +239,7 @@ void dequant_tp_had_quant_binding(
         reinterpret_cast<const __nv_fp8_e4m3*>(x_micro_scales.data()),
         x_global_scale.data(),
         *seed.data(),  fp4_max, fp8_max,
-        x.shape(0), x.shape(1) * 2);
+        x.shape(0), x.shape(1) * 2, stream);
 }
 
 
@@ -237,15 +247,17 @@ NB_MODULE(_quartet2, m) {
     m.def("group_transform_128", &group_transform_128_binding);
     m.def("group_transform_128_eden", &group_transform_128_eden_binding, nb::arg("out"), nb::arg("group_scales"),
         nb::arg("tensor_scale"), nb::arg("scratch"), nb::arg("max_scale"), nb::arg("h"), nb::arg("x"), nb::arg("seed"),
-        nb::arg("fp4_max") = 6, nb::arg("fp8_max") = 256, nb::arg("transpose") = false);
+        nb::arg("fp4_max") = 6, nb::arg("fp8_max") = 256, nb::arg("transpose") = false, nb::arg("stream") = 0);
 
     m.def("dequant_tp_had_quant", &dequant_tp_had_quant_binding,
         nb::arg("out"), nb::arg("out_group_scales"), nb::arg("out_tensor_scale"),
         nb::arg("scratch"), nb::arg("max_scale"), nb::arg("h"),
         nb::arg("x"), nb::arg("x_group_scales"), nb::arg("x_tensor_scale"),
-        nb::arg("seed"), nb::arg("fp4_max") = 6, nb::arg("fp8_max") = 256);
+        nb::arg("seed"), nb::arg("fp4_max") = 6, nb::arg("fp8_max") = 256, nb::arg("stream") = 0);
 
-    m.def("eden_fp4", &eden_fp4_binding);
-    m.def("four_six_fp4", &four_six_fp4_binding, nb::arg("out"), nb::arg("scales"), nb::arg("global_scale"), nb::arg("input"), nb::arg("amax"), nb::arg("scale_override"));
-    m.def("rtn_fp4", &rtn_fp4_binding, nb::arg("out"), nb::arg("scales"), nb::arg("global_scale"), nb::arg("input"), nb::arg("amax"), nb::arg("scale_override"));
+    m.def("eden_fp4", &eden_fp4_binding, nb::arg("out"), nb::arg("scales"), nb::arg("inp"),
+        nb::arg("amax_ptr"), nb::arg("scale_override"), nb::arg("seed"), nb::arg("stream") = 0);
+    
+    m.def("four_six_fp4", &four_six_fp4_binding, nb::arg("out"), nb::arg("scales"), nb::arg("global_scale"), nb::arg("input"), nb::arg("amax"), nb::arg("scale_override"), nb::arg("stream") = 0);
+    m.def("rtn_fp4", &rtn_fp4_binding, nb::arg("out"), nb::arg("scales"), nb::arg("global_scale"), nb::arg("input"), nb::arg("amax"), nb::arg("scale_override"), nb::arg("stream") = 0);
 }
diff --git a/kernels/csrc/dq_tp_q.cu b/kernels/csrc/dq_tp_q.cu
index 7175279..1f21efb 100644
--- a/kernels/csrc/dq_tp_q.cu
+++ b/kernels/csrc/dq_tp_q.cu
@@ -206,13 +206,13 @@ __global__ __launch_bounds__(NUM_WARPS*32, 1) void cutlass_dequant_tp_quant_kern
     }
 }
 
-void launch_eden_convert_scales_kernel(__nv_fp8_e4m3* scales_fp8, float* global_scale_ptr, const nv_bfloat16* scales_bf16, const unsigned* max_scale_ptr, long seed, int groups, float inv_fp8_max);
+void launch_eden_convert_scales_kernel(__nv_fp8_e4m3* scales_fp8, float* global_scale_ptr, const nv_bfloat16* scales_bf16, const unsigned* max_scale_ptr, long seed, int groups, float inv_fp8_max, cudaStream_t stream);
 
 void dequant_tp_quant_launcher(
     __nv_fp4x2_storage_t* y, __nv_fp8_e4m3* scales_fp8, float* global_scale_ptr,
     nv_bfloat16* scratch_scales, unsigned* max_scale, const nv_bfloat16* h,
     const __nv_fp4x2_storage_t* x, const __nv_fp8_e4m3* x_scales, const float* x_global_scale,
-    long seed, float fp4_max, float fp8_max, int M, int N)
+    long seed, float fp4_max, float fp8_max, int M, int N, cudaStream_t stream)
 {
     int groups = M * N / 128;
     int blocks, device;
@@ -224,19 +224,19 @@ void dequant_tp_quant_launcher(
     CUDA_CHECK(cudaOccupancyMaxActiveBlocksPerMultiprocessor(&blocks, cutlass_dequant_tp_quant_kernel, 32*NUM_WARPS, smem));
     int sms;
     CUDA_CHECK(cudaDeviceGetAttribute(&sms, cudaDevAttrMultiProcessorCount, device));
-    CUDA_CHECK(cudaMemset(max_scale, 0, sizeof(unsigned)));
+    CUDA_CHECK(cudaMemsetAsync(max_scale, 0, sizeof(unsigned), stream));
 
-    cutlass_dequant_tp_quant_kernel<<<sms * blocks, dim3(32, NUM_WARPS), smem>>>(
+    cutlass_dequant_tp_quant_kernel<<<sms * blocks, dim3(32, NUM_WARPS), smem, stream>>>(
         y, scratch_scales, max_scale, h, x, x_scales, x_global_scale, M, N, 1.f / fp4_max);
     CUDA_CHECK(cudaGetLastError());
-    launch_eden_convert_scales_kernel(scales_fp8, global_scale_ptr, scratch_scales, max_scale, seed, groups, 1.f / fp8_max);
+    launch_eden_convert_scales_kernel(scales_fp8, global_scale_ptr, scratch_scales, max_scale, seed, groups, 1.f / fp8_max, stream);
 }
 
 void dequant_tp_had_quant(
     __nv_fp4x2_storage_t* y, __nv_fp8_e4m3* scales_fp8, float* global_scale_ptr,
     nv_bfloat16* scratch_scales, unsigned* max_scale, const nv_bfloat16* h,
     const __nv_fp4x2_storage_t* x, const __nv_fp8_e4m3* x_scales, const float* x_global_scale,
-    long seed, float fp4_max, float fp8_max, int M, int N)
+    long seed, float fp4_max, float fp8_max, int M, int N, cudaStream_t stream)
 {
-    dequant_tp_quant_launcher(y, scales_fp8, global_scale_ptr, scratch_scales, max_scale, h, x, x_scales, x_global_scale, seed, fp4_max, fp8_max, M, N);
+    dequant_tp_quant_launcher(y, scales_fp8, global_scale_ptr, scratch_scales, max_scale, h, x, x_scales, x_global_scale, seed, fp4_max, fp8_max, M, N, stream);
 }
diff --git a/kernels/csrc/group_transform_and_eden.cu b/kernels/csrc/group_transform_and_eden.cu
index 1fcd329..f581147 100644
--- a/kernels/csrc/group_transform_and_eden.cu
+++ b/kernels/csrc/group_transform_and_eden.cu
@@ -252,7 +252,9 @@ __global__ void eden_convert_scales_kernel(__nv_fp8_e4m3* scales_fp8, float* glo
     dst_scales.store(scales_fp8 + i * 8);
 }
 
-void launch_eden_convert_scales_kernel(__nv_fp8_e4m3* scales_fp8, float* global_scale_ptr, const nv_bfloat16* scales_bf16, const unsigned* max_scale_ptr, long seed, int groups, float inv_fp8_max) {
+// ... (helper functions)
+
+void launch_eden_convert_scales_kernel(__nv_fp8_e4m3* scales_fp8, float* global_scale_ptr, const nv_bfloat16* scales_bf16, const unsigned* max_scale_ptr, long seed, int groups, float inv_fp8_max, cudaStream_t stream) {
     cudaLaunchAttribute attribute[1];
     attribute[0].id = cudaLaunchAttributeProgrammaticStreamSerialization;
     attribute[0].val.programmaticStreamSerializationAllowed = 1;
@@ -263,7 +265,7 @@ void launch_eden_convert_scales_kernel(__nv_fp8_e4m3* scales_fp8, float* global_
     config.blockDim = dim3(128);
     config.gridDim = dim3((groups + 127)/128);
     config.dynamicSmemBytes = 0;
-    config.stream = nullptr;
+    config.stream = stream;
     CUDA_CHECK(cudaLaunchKernelEx(&config, eden_convert_scales_kernel, scales_fp8, global_scale_ptr, scales_bf16, max_scale_ptr, seed, groups, inv_fp8_max));
 }
 
@@ -271,7 +273,7 @@ template<bool TransposeA>
 void group_transform_128_eden_launcher(
     __nv_fp4x2_storage_t* y, __nv_fp8_e4m3* scales_fp8, float* global_scale_ptr,
     nv_bfloat16* scratch_scales, unsigned* max_scale, const nv_bfloat16* h, const nv_bfloat16* x,
-    long seed, float fp4_max, float fp8_max, int M, int N)
+    long seed, float fp4_max, float fp8_max, int M, int N, cudaStream_t stream)
 {
     int groups = M * N / 128;
     int blocks, device;
@@ -283,21 +285,21 @@ void group_transform_128_eden_launcher(
     int sms;
     CUDA_CHECK(cudaDeviceGetAttribute(&sms, cudaDevAttrMultiProcessorCount, device));
 
-    CUDA_CHECK(cudaMemset(max_scale, 0, sizeof(unsigned)));
+    CUDA_CHECK(cudaMemsetAsync(max_scale, 0, sizeof(unsigned), stream));
 
-    cutlass_group_transform_128_eden_kernel<TransposeA><<<sms * blocks, dim3(32, NUM_WARPS), smem>>>(y, scratch_scales, max_scale, h, x, M, N, 1.f / fp4_max);
+    cutlass_group_transform_128_eden_kernel<TransposeA><<<sms * blocks, dim3(32, NUM_WARPS), smem, stream>>>(y, scratch_scales, max_scale, h, x, M, N, 1.f / fp4_max);
     CUDA_CHECK(cudaGetLastError());
-    launch_eden_convert_scales_kernel(scales_fp8, global_scale_ptr, scratch_scales, max_scale, seed, groups, 1.f / fp8_max);
+    launch_eden_convert_scales_kernel(scales_fp8, global_scale_ptr, scratch_scales, max_scale, seed, groups, 1.f / fp8_max, stream);
 }
 
 void group_transform_128_eden(
     __nv_fp4x2_storage_t* y, __nv_fp8_e4m3* scales_fp8, float* global_scale_ptr,
     nv_bfloat16* scratch_scales, unsigned* max_scale, const nv_bfloat16* h, const nv_bfloat16* x,
-    long seed, float fp4_max, float fp8_max, int M, int N, bool transposeX)
+    long seed, float fp4_max, float fp8_max, int M, int N, bool transposeX, cudaStream_t stream)
 {
     if (transposeX) {
-        group_transform_128_eden_launcher<true>(y, scales_fp8, global_scale_ptr, scratch_scales, max_scale, h, x, seed, fp4_max, fp8_max, M, N);
+        group_transform_128_eden_launcher<true>(y, scales_fp8, global_scale_ptr, scratch_scales, max_scale, h, x, seed, fp4_max, fp8_max, M, N, stream);
     } else {
-        group_transform_128_eden_launcher<false>(y, scales_fp8, global_scale_ptr, scratch_scales, max_scale, h, x, seed, fp4_max, fp8_max, M, N);
+        group_transform_128_eden_launcher<false>(y, scales_fp8, global_scale_ptr, scratch_scales, max_scale, h, x, seed, fp4_max, fp8_max, M, N, stream);
     }
 }
diff --git a/kernels/csrc/round_eden_fp4.cu b/kernels/csrc/round_eden_fp4.cu
index a5aae34..e0725d5 100644
--- a/kernels/csrc/round_eden_fp4.cu
+++ b/kernels/csrc/round_eden_fp4.cu
@@ -81,11 +81,11 @@ __global__ void eden_fp4_kernel(__nv_fp4x4_e2m1* y_ptr, __nv_fp8_e4m3* scale_ptr
     result.store(reinterpret_cast<unsigned char*>(y_ptr) + 4 * idx);
 }
 
-void eden_fp4(__nv_fp4x4_e2m1* y_ptr, __nv_fp8_e4m3* scale_ptr, const nv_bfloat16* x_ptr, const float* amax_ptr, float scale_override, long seed, long nelem) {
+void eden_fp4(__nv_fp4x4_e2m1* y_ptr, __nv_fp8_e4m3* scale_ptr, const nv_bfloat16* x_ptr, const float* amax_ptr, float scale_override, long seed, long nelem, cudaStream_t stream) {
     if (nelem % 8 != 0) throw std::runtime_error("eden_fp4_fake: nelem must be divisible by 8");
     int n_vecs = nelem / 8;
     int block_size = 256;
     int n_blocks = (n_vecs + block_size - 1) / block_size;
-    eden_fp4_kernel<<<n_blocks, block_size>>>(y_ptr, scale_ptr, x_ptr, amax_ptr, scale_override, seed, n_vecs);
+    eden_fp4_kernel<<<n_blocks, block_size, 0, stream>>>(y_ptr, scale_ptr, x_ptr, amax_ptr, scale_override, seed, n_vecs);
     CUDA_CHECK(cudaGetLastError());
 }
diff --git a/kernels/csrc/round_four_six.cu b/kernels/csrc/round_four_six.cu
index 4ab1f54..344ca7a 100644
--- a/kernels/csrc/round_four_six.cu
+++ b/kernels/csrc/round_four_six.cu
@@ -114,20 +114,22 @@ __global__ void four_six_fp4_kernel(__nv_fp4x4_e2m1* y_ptr, __nv_fp8_e4m3* scale
     }
 }
 
-void four_six_fp4(__nv_fp4x4_e2m1* y_ptr, __nv_fp8_e4m3* scale_ptr, float* global_scale_ptr, const nv_bfloat16* x_ptr, const float* amax_ptr, float scale_override, long nelem) {
+// ... (kernel definition)
+
+void four_six_fp4(__nv_fp4x4_e2m1* y_ptr, __nv_fp8_e4m3* scale_ptr, float* global_scale_ptr, const nv_bfloat16* x_ptr, const float* amax_ptr, float scale_override, long nelem, cudaStream_t stream) {
     if (nelem % 8 != 0) throw std::runtime_error("four_six_fp4: nelem must be divisible by 8");
     int n_vecs = nelem / 8;
     int block_size = 256;
     int n_blocks = (n_vecs + block_size - 1) / block_size;
-    four_six_fp4_kernel<6.f, 4.f><<<n_blocks, block_size>>>(y_ptr, scale_ptr, global_scale_ptr, x_ptr, amax_ptr, 1.f / scale_override, n_vecs);
+    four_six_fp4_kernel<6.f, 4.f><<<n_blocks, block_size, 0, stream>>>(y_ptr, scale_ptr, global_scale_ptr, x_ptr, amax_ptr, 1.f / scale_override, n_vecs);
     CUDA_CHECK(cudaGetLastError());
 }
 
-void rtn_fp4(__nv_fp4x4_e2m1* y_ptr, __nv_fp8_e4m3* scale_ptr, float* global_scale_ptr, const nv_bfloat16* x_ptr, const float* amax_ptr, float scale_override, long nelem) {
+void rtn_fp4(__nv_fp4x4_e2m1* y_ptr, __nv_fp8_e4m3* scale_ptr, float* global_scale_ptr, const nv_bfloat16* x_ptr, const float* amax_ptr, float scale_override, long nelem, cudaStream_t stream) {
     if (nelem % 8 != 0) throw std::runtime_error("rtn_fp4: nelem must be divisible by 8");
     int n_vecs = nelem / 8;
     int block_size = 256;
     int n_blocks = (n_vecs + block_size - 1) / block_size;
-    four_six_fp4_kernel<6.f><<<n_blocks, block_size>>>(y_ptr, scale_ptr, global_scale_ptr, x_ptr, amax_ptr, 1.f / scale_override, n_vecs);
+    four_six_fp4_kernel<6.f><<<n_blocks, block_size, 0, stream>>>(y_ptr, scale_ptr, global_scale_ptr, x_ptr, amax_ptr, 1.f / scale_override, n_vecs);
     CUDA_CHECK(cudaGetLastError());
 }
diff --git a/kernels/python/quartet2/quant.py b/kernels/python/quartet2/quant.py
index d1886e2..7de318e 100644
--- a/kernels/python/quartet2/quant.py
+++ b/kernels/python/quartet2/quant.py
@@ -6,18 +6,22 @@ from . import _quartet2
 # torch-compile compatible wrapping
 @torch.library.custom_op("clover::four_six", mutates_args=("o", "s", "t"))
 def _four_six_fp4_op(o: torch.Tensor, s: torch.Tensor, t: torch.Tensor, x: torch.Tensor, amax: torch.Tensor, scale_override: float) -> None:
-    _quartet2.four_six_fp4(o, s.view(torch.uint8), t, x.detach(), amax, scale_override)
+    stream = torch.cuda.current_stream(x.device).cuda_stream
+    _quartet2.four_six_fp4(o, s.view(torch.uint8), t, x.detach(), amax, scale_override, stream)
 
 
 @torch.library.custom_op("clover::rtn", mutates_args=("o", "s", "t"))
 def _rtn_fp4_op(o: torch.Tensor, s: torch.Tensor, t: torch.Tensor, x: torch.Tensor, amax: torch.Tensor, scale_override: float) -> None:
-    _quartet2.rtn_fp4(o, s.view(torch.uint8), t, x.detach(), amax, scale_override)
+    stream = torch.cuda.current_stream(x.device).cuda_stream
+    _quartet2.rtn_fp4(o, s.view(torch.uint8), t, x.detach(), amax, scale_override, stream)
 
 
 @torch.library.custom_op("clover::group_transform_eden", mutates_args=("o", "s", "t", "scratch_scales", "scratch_max"))
 def _group_transform_128_eden(o: torch.Tensor, s: torch.Tensor, t: torch.Tensor,
                               scratch_scales: torch.Tensor, scratch_max: torch.Tensor,
                               h: torch.Tensor, x: torch.Tensor, seed: torch.Tensor, fp4_max: float, fp8_max: float, transpose: bool) -> None:
+    stream = torch.cuda.current_stream(x.device).cuda_stream
+    print(f"DEBUG: stream={stream}")
     _quartet2.group_transform_128_eden(
         o,
         s.view(torch.uint8),
@@ -30,6 +34,7 @@ def _group_transform_128_eden(o: torch.Tensor, s: torch.Tensor, t: torch.Tensor,
         fp4_max=fp4_max,
         fp8_max=fp8_max,
         transpose=transpose,
+        stream=stream
     )
 
 
@@ -38,6 +43,7 @@ def _dequant_tp_had_quant(o: torch.Tensor, s: torch.Tensor, t: torch.Tensor,
                           scratch_scales: torch.Tensor, scratch_max: torch.Tensor,
                           h: torch.Tensor, x: torch.Tensor, x_group_scales: torch.Tensor, x_tensor_scale: torch.Tensor,
                           seed: torch.Tensor, fp4_max: float, fp8_max: float) -> None:
+    stream = torch.cuda.current_stream(x.device).cuda_stream
     _quartet2.dequant_tp_had_quant(
         o,
         s,
@@ -51,6 +57,7 @@ def _dequant_tp_had_quant(o: torch.Tensor, s: torch.Tensor, t: torch.Tensor,
         seed,
         fp4_max=fp4_max,
         fp8_max=fp8_max,
+        stream=stream
     )
 
 
