# Apex attribution

`multi_tensor_apply.cuh` and `compat.h` are adapted from NVIDIA Apex's `csrc/`
directory and used under the adjacent `APEX_LICENSE`. The optimizer functor,
stateless stochastic-rounding implementation, validation, and Python
integration are original to this repository.

Upstream reference: <https://github.com/NVIDIA/apex>
